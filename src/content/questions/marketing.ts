import type { Question } from "@/lib/questions/types";

/**
 * Marketing.
 *
 * Intentionally empty. The seed questions that used to live here were written
 * from the syllabus, not from real exams, and were removed on 2026-08-21.
 * This bank only accepts questions derived from actual TUM exams - use the
 * `add-exam-questions` skill; every entry must carry a `source`.
 */
export const marketingQuestions: Question[] = [];
